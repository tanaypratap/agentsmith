# Reflections — `<REPO>`

The deep, durable retrospective for this repo. Owned by all PMs who work here.

This is Reflections **layer 2**. Layer 1 is the PM board's `## Learnings` — tactical and in-the-moment. When a layer-1 learning recurs, or proves it is not a one-off, promote it here as a considered reflection. See `.agentsmith/methodology/collaboration.md`.

Committed and durable — unlike the board, this travels with the repo.

---

## `<DATE>` — `<reflection title>`

**What happened:** `<the situation>`

**What we learned:** `<the durable lesson>`

**How we work differently now:** `<the change in practice>`

# PM Board

Shared coordination board for PMs working on `<REPO>`.
Last touched: `<DATE>` by `<PM>` (`<one-line summary>`).

GitHub issues remain the source of truth for actual work. This board exists so PMs do not trip over each other.

## Active PMs

| Name | Lane | Status |
|------|------|--------|
| `<PM name>` | `<what they own>` | `<active / paused>` |

## Current Focus

### `<PM name>`
- `<what this PM is on right now>`

## Messages

Append-only, dated, attributed. Never edit another PM's message — reply with a new one.

### `<DATE>` — `<From>` → `<To>`

`<message>`

## Wins

- `<DATE>`: `<notable shipped work>`

## Learnings

Tactical, in-the-moment lessons — Reflections layer 1. When one recurs, promote it into `.agentsmith/reflections.md`.

- `<DATE>` (`<PM>`): `<learning>`

## Open Asks

| # | From | To | Ask | Status |
|---|------|----|----|--------|
| 1 | `<PM>` | `<PM>` | `<request>` | open |

## Conventions

- This board lives at `.agentsmith/tmp/pm-board.md` — gitignored scratch, never committed.
- Each PM owns its Active PMs row and its Current Focus section.
- Messages, Wins, Learnings, and Open Asks are shared — anyone may add.
- Mark an Open Ask `closed` when resolved; never delete it — the trail is the point.

# Start Here

You were pointed at a repo's `.agentsmith/`. Route yourself — this file does nothing else.

1. **Is this repo set up?** Check that `.agentsmith/` exists and contains `pm-setup.md` and `methodology/`.
2. **Yes — it is set up** → read `pm-setup.md` and follow it. You are starting a PM session.
3. **No — not set up** (you are reading this from the agentsmith repo itself, or `.agentsmith/` is incomplete) → this repo needs first-time setup. Go to the agentsmith repo and follow its `setup.md`.

That is all. Do not do setup work from here. If step 2 applies, do **not** re-fetch the agentsmith repo — everything you need is already local in `.agentsmith/`.
